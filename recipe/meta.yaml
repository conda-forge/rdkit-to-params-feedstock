{% set name = "rdkit-to-params" %}
{% set version = "1.3.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/rdkit_to_params-{{ version }}.tar.gz
  sha256: 7a66bff3b9e432d1b661f8725f72172f1159c708c0d3ef9f01c12f93372859a2

build:
  entry_points:
    - smiles-to-params=rdkit_to_params.cli:smiles
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - rdkit

test:
  imports:
    - rdkit_to_params
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/matteoferla/rdkit_to_params
  summary: Create or modify Rosetta params files (topology files) from scratch, RDKit mols or another params file.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
